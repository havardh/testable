import sinon from "sinon";

import * as service from "../service";

describe("Service", () => {

  test("should return 20 tweets", () => {
    // Step 6
    // Call the service and verify that the results
    // contains a list of 20 tweets
  });
});
